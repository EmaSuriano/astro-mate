---
import { marked } from "marked";

type Props = {
  content: string;
};

const { content } = Astro.props;
const html = marked.parse(content);
---

<article class="prose max-w-none prose-a:text-secondary" set:html={html} />
